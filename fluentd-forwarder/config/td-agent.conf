<system>
  @log_level debug
</system>

# <source>
#   @type tail

#   path /var/log/app/statistics.counter.log
#   pos_file /var/log/td-agent/counter.log.pos
#   tag counter_log.frontend
#   format json
# </source>

<match **>
  @type forward

  <server>
    host 172.30.0.10
  </server>
  <secondary>
    @type file
    path /var/log/td-agent/forward-failed
  </secondary>
</match>
